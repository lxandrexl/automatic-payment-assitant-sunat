# --- apps/web (todas server-side; la API_KEY nunca llega al browser) ---
# URL interna de la API en la red de compose.
API_URL=http://api:3001
# Misma API_KEY que apps/api (para las mutaciones proxeadas).
API_KEY=
