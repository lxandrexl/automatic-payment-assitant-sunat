{
  "name": "tributo",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @tributo/core build",
    "lint": "eslint .",
    "typecheck": "pnpm run build && pnpm -r typecheck",
    "test": "pnpm run build && pnpm -r test"
  },
  "devDependencies": {
    "eslint": "^9.31.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.36.0"
  }
}
